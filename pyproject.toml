[project]
name = "cudmore-projects-cv"
version = "0.1.0"
description = "MapManager project portfolio site"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mkdocs>=1.6,<2",
    "mkdocs-material>=9.5,<10",
    "mkdocs-macros-plugin>=1,<2",
]

[tool.uv]
package = false
